# jedox-file-transformer
n in-memory, recursive transformation engine for Jedox .pb and .wss files. This tool allows users to deep-scan nested archives, perform individual or global word replacements, and manually edit internal XML/RELS components through a Streamlit-based UI while maintaining original file structures.
